# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: false
  weight: 8
  showOnNavbar: false
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
